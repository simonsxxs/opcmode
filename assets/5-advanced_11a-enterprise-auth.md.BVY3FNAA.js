import{_ as t,c as a,o as c,a5 as n,j as e,a as o,bg as l,bh as r,bi as p}from"./chunks/framework.Dtwt352Q.js";const _=JSON.parse('{"title":"5.11a 企业认证集成","description":"对接公司统一认证，把登录时保存的 token 注入到 OpenCode 进程环境变量，并下发组织默认配置（短期 token 用插件实现续期）。","frontmatter":{"title":"5.11a 企业认证集成","subtitle":"对接自有统一认证与组织默认配置","course":"OpenCode 中文实战课","stage":"第五阶段","lesson":"5.11a","duration":"30 分钟","practice":"35 分钟","level":"进阶","description":"对接公司统一认证，把登录时保存的 token 注入到 OpenCode 进程环境变量，并下发组织默认配置（短期 token 用插件实现续期）。","tags":["企业","认证","配置","插件"],"prerequisite":["5.1 配置全解","5.11 企业版功能"]},"headers":[],"relativePath":"5-advanced/11a-enterprise-auth.md","filePath":"5-advanced/11a-enterprise-auth.md","lastUpdated":1772014034000}'),i={name:"5-advanced/11a-enterprise-auth.md"};function s(h,d,u,g,k,m){return c(),a("div",null,[...d[0]||(d[0]=[n('<h1 id="企业认证集成" tabindex="-1">企业认证集成 <a class="header-anchor" href="#企业认证集成" aria-label="Permalink to “企业认证集成”">​</a></h1><blockquote><p>💡 <strong>一句话总结</strong>：把公司统一登录拿到的 Token 接进 OpenCode，让团队不用手填 Key，默认就能走内网模型干活。</p></blockquote><h2 id="📝-课程笔记" tabindex="-1">📝 课程笔记 <a class="header-anchor" href="#📝-课程笔记" aria-label="Permalink to “📝 课程笔记”">​</a></h2><p>本课核心知识点整理：</p><p><img src="'+l+'" alt="5.11a 企业认证集成学霸笔记" data-zoom-src="/images/5-advanced/enterprise-auth-notes.jpeg"></p><hr><p>你可以把这节课当成一件事：<strong>让 OpenCode 在公司内网“开箱即用”</strong>。</p><p>本课会给你 3 条路（从最省事到最灵活）：</p><p>1）环境变量：你们自己注入 Token，OpenCode 读取即可</p><p>2）well-known：登录时执行一次 <code>auth.command</code> 保存 token；后续启动只注入已保存 token + 拉取组织默认配置（不自动续期）</p><p>3）认证插件：需要 OAuth/刷新/签名/改 headers/params 时，用插件实现</p><div class="tip custom-block"><p class="custom-block-title">💡 本课怎么读最快</p><ul><li>先看“3 种接入方式对比图”，选你们团队最像的那条路</li><li>然后直接跳到对应步骤：方式 A（环境变量）/ 方式 B（well-known）/ 方式 C（插件认证）</li></ul></div><h2 id="学完你能做什么" tabindex="-1">学完你能做什么 <a class="header-anchor" href="#学完你能做什么" aria-label="Permalink to “学完你能做什么”">​</a></h2><ul><li>把公司统一认证的 Token 接进 OpenCode（不用每个人手填 Key）</li><li>用 <code>/.well-known/opencode</code> 给团队下发默认配置（比如强制走内部 AI 网关）</li><li>分清楚：什么时候用环境变量就够了，什么时候用 well-known，什么时候必须写认证插件</li></ul><p><img src="'+r+'" alt="三种接入方式对比" data-zoom-src="/images/5-advanced/11a-auth-3ways.jpeg"></p><hr><h2 id="你现在的困境" tabindex="-1">你现在的困境 <a class="header-anchor" href="#你现在的困境" aria-label="Permalink to “你现在的困境”">​</a></h2><ul><li>公司有统一认证（SSO/统一网关），但 OpenCode 默认是按 Provider Key 来配</li><li>让每个同学自己复制粘贴 Token：麻烦、易过期、也容易泄漏</li><li>你希望：团队机器一跑 OpenCode，就自动带上公司凭据，并且默认只用内网模型</li></ul><hr><h2 id="什么时候用这一招" tabindex="-1">什么时候用这一招 <a class="header-anchor" href="#什么时候用这一招" aria-label="Permalink to “什么时候用这一招”">​</a></h2><ul><li>你们能用一条命令获取 Token（例如 <code>corpctl token</code>、<code>sso login --print-token</code>）</li><li>你们有一个内部域名能提供 <code>https://&lt;host&gt;/.well-known/opencode</code></li><li>你希望把“默认模型、默认 Provider、禁用外网 Provider”等配置集中管理</li></ul><hr><h2 id="🎒-开始前的准备" tabindex="-1">🎒 开始前的准备 <a class="header-anchor" href="#🎒-开始前的准备" aria-label="Permalink to “🎒 开始前的准备”">​</a></h2><ul><li>[ ] 读过 <a href="./01a-config-basics.html">5.1 配置全解</a></li><li>[ ] 准备一个内部域名，例如 <code>https://ai.company.internal</code></li><li>[ ] 准备一个获取 Token 的命令（推荐非交互：直接输出 token 到 stdout，不依赖 stdin 交互）</li></ul><div class="info custom-block"><p class="custom-block-title">ℹ️ 这里的“Token”指什么？</p><p>OpenCode 的 Provider 最终需要的通常就是一段字符串：API Key、Bearer Token、或网关发的临时凭据。</p><p>这节课的目标是：把这段字符串自动放进某个环境变量里，让 OpenCode 的 Provider 能读到它。</p></div><hr><h2 id="核心思路" tabindex="-1">核心思路 <a class="header-anchor" href="#核心思路" aria-label="Permalink to “核心思路”">​</a></h2><p>OpenCode 有一个很适合企业内网的入口：<code>opencode auth login &lt;url&gt;</code>。</p><ul><li>它会请求 <code>&lt;url&gt;/.well-known/opencode</code>，读到 <code>auth.command</code> 和 <code>auth.env</code>，然后在本机执行 <code>auth.command</code> 取回 Token 并保存（源码：<code>packages/opencode/src/cli/cmd/auth.ts</code>）。</li><li>OpenCode 启动加载配置时，如果发现你保存过这种 <code>type: &quot;wellknown&quot;</code> 的凭据，它会在运行时设置 <code>process.env[auth.env]=token</code>，并把 <code>&lt;url&gt;/.well-known/opencode</code> 里的 <code>config</code> 当成“组织默认配置”合并进来（源码：<code>packages/opencode/src/config/config.ts</code>）。</li></ul><p>企业里最常见的对接方式，其实有 3 种（按“需要你改多少东西”从少到多）：</p><ol><li>环境变量：你们自己把 Token 注入到环境变量，OpenCode 只负责读取</li><li>well-known：OpenCode 负责“获取 Token + 注入 env + 下发默认配置”</li><li>认证插件：当你们需要 OAuth/刷新/签名/改请求头时，用插件实现登录流程和请求改写</li></ol><div class="info custom-block"><p class="custom-block-title">🤔 什么是 well-known？</p><p><code>/.well-known/</code> 是一个约定俗成的“固定目录”，常用来放<strong>服务发现</strong>信息：客户端只要访问一个固定路径，就能拿到机器可读的配置。</p><p>OpenCode 在企业场景里约定了一个端点：<code>/.well-known/opencode</code>。</p><p>它返回一个 JSON，通常包含两块：</p><ul><li><code>auth</code>：告诉 OpenCode 用什么命令拿到 Token（<code>auth.command</code>），以及把 Token 注入到哪个环境变量（<code>auth.env</code>）</li><li><code>config</code>：组织默认配置（会被合并进 OpenCode 的配置系统）</li></ul><p>注意：OpenCode 会直接运行 <code>auth.command</code>（源码：<code>packages/opencode/src/cli/cmd/auth.ts</code>）。而且启动时也会拉取 <code>config</code> 并合并（源码：<code>packages/opencode/src/config/config.ts</code>）。</p><p><code>config</code> 里如果包含 <code>plugin</code>，OpenCode 可能会自动安装插件包并 <code>import()</code> 执行（源码：<code>packages/opencode/src/plugin/index.ts</code>）。</p><p>简单说：把 <code>/.well-known/opencode</code> 当成“组织发给开发机的配置入口”，只应该部署在你们可控、可信的内网域名上。</p></div><details class="details custom-block"><summary>📦 well-known 下发的是“默认值”，不是“强制策略”</summary><p>OpenCode 的配置合并顺序（低 → 高）是：</p><p>1）远程 <code>/.well-known/opencode</code>（组织默认）</p><p>2）全局配置（<code>~/.config/opencode/{config.json,opencode.json,opencode.jsonc}</code>）</p><p>3）自定义配置路径（<code>OPENCODE_CONFIG</code>）</p><p>4）项目配置（<code>opencode.json{,c}</code>）</p><p>5）<code>.opencode/</code> 目录配置（包含 <code>.opencode/opencode.json{,c}</code> 和 <code>.opencode/plugin/</code> 等）</p><p>5.1）你也可以用 <code>OPENCODE_CONFIG_DIR</code> 指定一个额外的配置目录（它会被当成目录来源的一部分加载，源码：<code>packages/opencode/src/config/config.ts</code>）</p><p>6）内联配置（<code>OPENCODE_CONFIG_CONTENT</code>）</p><p>（企业版）managed config 目录会覆盖以上所有来源（源码注释：<code>packages/opencode/src/config/config.ts</code>）。</p><p>所以 <code>wellknown.config</code> 更适合做“团队默认开箱体验”。</p><p>但有个例外：<code>plugin</code>/<code>instructions</code> 这类数组字段在合并时是“合并并去重”，不是简单覆盖（源码：<code>packages/opencode/src/config/config.ts</code> 的 merge 逻辑：用 <code>Set</code> 按完整字符串值去重）。</p><p>其中 <code>plugin</code> 还会再做一次“按插件名去重”，并且高优先级来源会覆盖低优先级来源的同名插件（源码：<code>packages/opencode/src/config/config.ts</code> 的 <code>deduplicatePlugins()</code>）。</p><p>如果你要强制所有人只能走内网网关，通常需要企业版集中管理，或配合 IT/镜像/权限策略把本机覆盖入口限制住。</p></details><hr><h2 id="跟我做" tabindex="-1">跟我做 <a class="header-anchor" href="#跟我做" aria-label="Permalink to “跟我做”">​</a></h2><h3 id="第-1-步-先选你要用的接入方式" tabindex="-1">第 1 步：先选你要用的接入方式 <a class="header-anchor" href="#第-1-步-先选你要用的接入方式" aria-label="Permalink to “第 1 步：先选你要用的接入方式”">​</a></h3><p><strong>为什么</strong><br> 选对方式，后面就不会走冤枉路。</p><table tabindex="0"><thead><tr><th>你们的现状</th><th>选哪种</th></tr></thead><tbody><tr><td>已经有固定 Key（或你能在 K8s/CI 里很方便注入 Token）</td><td>环境变量</td></tr><tr><td>能用一条命令输出 Token，且想把默认配置统一下发</td><td>well-known</td></tr><tr><td>必须走 OAuth/设备码/需要自动刷新/需要签名或改 headers/params</td><td>认证插件</td></tr></tbody></table><h3 id="第-2-步-方式-a-只用环境变量接入-最省事" tabindex="-1">第 2 步（方式 A）：只用环境变量接入（最省事） <a class="header-anchor" href="#第-2-步-方式-a-只用环境变量接入-最省事" aria-label="Permalink to “第 2 步（方式 A）：只用环境变量接入（最省事）”">​</a></h3><p><strong>为什么</strong><br> 如果你们已经能把 Token 放到环境变量里（本机 / K8s / CI），OpenCode 不需要任何额外认证机制。</p><p>1）在 <code>opencode.json</code> 配一个内网 provider，并明确它从哪个环境变量读 key：</p>',41),e("div",{class:"language-jsonc"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"jsonc"),e("pre",null,[e("code",{"v-pre":""},`// opencode.json
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "corp-gateway": {
      "name": "Company AI Gateway",
      "env": ["COMPANY_AI_TOKEN"],
      "api": "https://ai-gateway.company.internal/v1",
      "npm": "@ai-sdk/openai-compatible",
      "models": {
        "qwen2.5-72b": {
          "name": "Qwen 2.5 72B",
          "tool_call": true,
          "reasoning": true,
          "temperature": true,
          "limit": { "context": 128000, "output": 8192 }
        }
      }
    }
  },
  "model": "corp-gateway/qwen2.5-72b"
}`)])],-1),e("p",null,"2）运行前注入环境变量并验证：",-1),e("div",{class:"vp-code-group"},[e("div",{class:"tabs"},[e("input",{type:"radio",name:"group-314",id:"tab-315",checked:""}),e("label",{"data-title":"macOS/Linux",for:"tab-315"},"macOS/Linux"),e("input",{type:"radio",name:"group-314",id:"tab-316"}),e("label",{"data-title":"Windows PowerShell",for:"tab-316"},"Windows PowerShell")]),e("div",{class:"blocks"},[e("div",{class:"language-bash active"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"bash"),e("pre",null,[e("code",{"v-pre":""},`export COMPANY_AI_TOKEN="<your-token>"
opencode run -m corp-gateway/qwen2.5-72b "ping"`)])]),e("div",{class:"language-powershell"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"powershell"),e("pre",null,[e("code",{"v-pre":""},`$env:COMPANY_AI_TOKEN = "<your-token>"
opencode run -m corp-gateway/qwen2.5-72b "ping"`)])])])],-1),n('<p><strong>你应该看到</strong>：<code>opencode run</code> 能返回模型输出。</p><div class="tip custom-block"><p class="custom-block-title">💡 适合 K8s/CI</p><p>把 <code>COMPANY_AI_TOKEN</code> 做成 K8s Secret 或 CI Secret 注入就行，不需要每个人在本机登录。</p></div><h3 id="第-3-步-方式-b-用-well-known-做-统一登录-下发组织配置" tabindex="-1">第 3 步（方式 B）：用 well-known 做“统一登录 + 下发组织配置” <a class="header-anchor" href="#第-3-步-方式-b-用-well-known-做-统一登录-下发组织配置" aria-label="Permalink to “第 3 步（方式 B）：用 well-known 做“统一登录 + 下发组织配置””">​</a></h3><p><strong>为什么</strong><br> well-known 把“拿 Token + 默认配置”集中到一个组织入口：<code>https://&lt;host&gt;/.well-known/opencode</code>。</p><div class="warning custom-block"><p class="custom-block-title">⚠️ Token 续期的边界</p><p>well-known 的 <code>auth.command</code> 只在你执行 <code>opencode auth login &lt;url&gt;</code> 时运行一次，并把 token 保存到本机。</p><p>后续启动 OpenCode 时，只会注入这份“已保存的 token”，不会自动刷新/续期。</p><p>如果你们的 token 很短期（比如 1 小时过期），更适合走方式 C：用插件实现刷新/签名流程。</p></div><p><img src="'+p+'" alt="well-known 工作流" data-zoom-src="/images/5-advanced/11a-auth-wellknown-flow.jpeg"></p><h4 id="_3-1-准备-well-known-opencode-响应" tabindex="-1">3.1 准备 <code>/.well-known/opencode</code> 响应 <a class="header-anchor" href="#_3-1-准备-well-known-opencode-响应" aria-label="Permalink to “3.1 准备 /.well-known/opencode 响应”">​</a></h4><p><strong>为什么</strong><br><code>opencode auth login &lt;url&gt;</code> 的行为完全由这个 JSON 驱动：它告诉 OpenCode 用什么命令获取 Token、把 Token 放到哪个环境变量，以及要下发哪些组织默认配置。</p><p>下面是一个最小可用的示例（你们需要在 <code>https://ai.company.internal/.well-known/opencode</code> 返回它）：</p>',9),e("div",{class:"language-jsonc"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"jsonc"),e("pre",null,[e("code",{"v-pre":""},`{
  "auth": {
    "command": ["corpctl", "ai", "token", "--format=plain"],
    "env": "COMPANY_AI_TOKEN"
  },
  "config": {
    "$schema": "https://opencode.ai/config.json",

    // 可选：强制禁用外网 Provider（示例）
    "disabled_providers": ["openai", "anthropic", "openrouter"],

    // 配置公司内部 AI 网关（OpenAI Compatible）
    "provider": {
      "corp-gateway": {
        "name": "Company AI Gateway",
        "env": ["COMPANY_AI_TOKEN"],
        "api": "https://ai-gateway.company.internal/v1",
        "npm": "@ai-sdk/openai-compatible",
        "models": {
          "qwen2.5-72b": {
            "name": "Qwen 2.5 72B",
            "tool_call": true,
            "reasoning": true,
            "temperature": true,
            "limit": { "context": 128000, "output": 8192 }
          }
        }
      }
    },

    // 让团队默认就用内网模型
    "model": "corp-gateway/qwen2.5-72b"
  }
}`)])],-1),n('<p><strong>你应该看到</strong>：浏览器访问 <code>https://ai.company.internal/.well-known/opencode</code> 能返回上面的 JSON。</p><div class="warning custom-block"><p class="custom-block-title">⚠️ 关键点</p><ul><li><code>auth.command</code> 必须能在开发机/容器里运行；它的 stdout 会被当成 Token。</li><li><code>auth.env</code> 是环境变量名。OpenCode 启动时会在当前进程里设置 <code>process.env[auth.env]=token</code>（不等同于写入你的系统全局环境变量）。</li></ul></div><h4 id="_3-2-用-opencode-auth-login-把凭据写入本机" tabindex="-1">3.2 用 <code>opencode auth login</code> 把凭据写入本机 <a class="header-anchor" href="#_3-2-用-opencode-auth-login-把凭据写入本机" aria-label="Permalink to “3.2 用 opencode auth login 把凭据写入本机”">​</a></h4><p><strong>为什么</strong><br> 这一步会把 <code>type: &quot;wellknown&quot;</code> 的凭据落到本机的 <code>auth.json</code> 里。之后每次启动 OpenCode，它都会自动注入环境变量并拉取远程 <code>config</code>。</p>',4),e("div",{class:"language-bash"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"bash"),e("pre",null,[e("code",{"v-pre":""},"opencode auth login https://ai.company.internal")])],-1),n('<p><strong>你应该看到</strong>：终端提示类似：</p><ul><li><code>Running \\</code>corpctl ai token --format=plain``</li><li><code>Logged into https://ai.company.internal</code></li></ul><h4 id="_3-3-确认凭据已经存在" tabindex="-1">3.3 确认凭据已经存在 <a class="header-anchor" href="#_3-3-确认凭据已经存在" aria-label="Permalink to “3.3 确认凭据已经存在”">​</a></h4><p><strong>为什么</strong><br> 你要先确认 OpenCode 真的保存了 well-known 凭据，避免后面“怎么配置都不生效”。</p>',4),e("div",{class:"language-bash"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"bash"),e("pre",null,[e("code",{"v-pre":""},"opencode auth list")])],-1),n('<p><strong>你应该看到</strong>：列表里包含一条 <code>https://ai.company.internal wellknown</code>（provider 名称可能直接显示成 URL）。</p><h4 id="_3-4-验证组织默认配置已生效-跑一次-run" tabindex="-1">3.4 验证组织默认配置已生效（跑一次 <code>run</code>） <a class="header-anchor" href="#_3-4-验证组织默认配置已生效-跑一次-run" aria-label="Permalink to “3.4 验证组织默认配置已生效（跑一次 run）”">​</a></h4><p><strong>为什么</strong><br> 最快的验证方式是：直接跑一次 <code>opencode run</code>，看它能否用你们的内网模型完成任务。</p>',3),e("div",{class:"language-bash"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"bash"),e("pre",null,[e("code",{"v-pre":""},'opencode run -m corp-gateway/qwen2.5-72b "用 3 句话解释这段话的要点：……"')])],-1),n('<p><strong>你应该看到</strong>：能正常返回模型输出；如果失败，通常会提示认证/模型不存在/网络错误。</p><h3 id="第-4-步-方式-c-写插件对接统一认证-最灵活" tabindex="-1">第 4 步（方式 C）：写插件对接统一认证（最灵活） <a class="header-anchor" href="#第-4-步-方式-c-写插件对接统一认证-最灵活" aria-label="Permalink to “第 4 步（方式 C）：写插件对接统一认证（最灵活）”">​</a></h3><p><strong>为什么</strong><br> 当你们不是“跑命令吐 Token”，而是需要 OAuth、自动刷新、请求签名、动态改 headers/params，插件是最靠谱的落点。</p><h4 id="_4-1-写一个认证插件骨架" tabindex="-1">4.1 写一个认证插件骨架 <a class="header-anchor" href="#_4-1-写一个认证插件骨架" aria-label="Permalink to “4.1 写一个认证插件骨架”">​</a></h4><p>把文件放到 <code>.opencode/plugin/corp-auth.ts</code>：</p>',5),e("div",{class:"language-ts"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"ts"),e("pre",null,[e("code",{"v-pre":""},`import type { Plugin } from "@opencode-ai/plugin"

const plugin: Plugin = async () => {
  return {
    auth: {
      // 这里要和你配置的 provider id 一致
      provider: "corp-gateway",
      methods: [
        {
          type: "api",
          label: "Login with Company SSO",
          async authorize() {
            // 换成你们的取 token 逻辑：命令行/HTTP/本地缓存等
            const token = "<replace-me>"
            return { type: "success", key: token }
          },
        },
      ],
      async loader(getAuth) {
        const auth = await getAuth()
        if (!auth || auth.type !== "api") return {}
        return {
          apiKey: auth.key,
          // headers: { "X-Tenant": "..." },
        }
      },
    },

    // 真要改 headers/params/签名，走这些 hook 更直接
    // "chat.headers": async (_input, output) => { output.headers["X-Sign"] = "..." },
    // "chat.params": async (_input, output) => { output.options["seed"] = 1 },
  }
}

export default plugin`)])],-1),e("h4",{id:"_4-2-让-opencode-加载你的插件",tabindex:"-1"},[o("4.2 让 OpenCode 加载你的插件 "),e("a",{class:"header-anchor",href:"#_4-2-让-opencode-加载你的插件","aria-label":"Permalink to “4.2 让 OpenCode 加载你的插件”"},"​")],-1),e("p",null,[e("strong",null,"推荐做法"),o("：你把插件文件放到 "),e("code",null,".opencode/plugin/"),o(" 目录后，OpenCode 会自动扫描并加载（不需要额外写 "),e("code",null,"opencode.json"),o("）。")],-1),e("details",{class:"details custom-block"},[e("summary",null,"📦 我就是想写进 opencode.json"),e("p",null,[o("可以写相对路径，让 OpenCode 在加载配置时解析成可导入的 "),e("code",null,"file://..."),o(" URL：")]),e("div",{class:"language-jsonc"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"jsonc"),e("pre",null,[e("code",{"v-pre":""},`// opencode.json
{
  "plugin": ["./.opencode/plugin/corp-auth.ts"]
}`)])])],-1),e("h4",{id:"_4-3-用-opencode-auth-login-走插件登录",tabindex:"-1"},[o("4.3 用 "),e("code",null,"opencode auth login"),o(" 走插件登录 "),e("a",{class:"header-anchor",href:"#_4-3-用-opencode-auth-login-走插件登录","aria-label":"Permalink to “4.3 用 opencode auth login 走插件登录”"},"​")],-1),e("div",{class:"language-bash"},[e("button",{title:"Copy Code",class:"copy"}),e("span",{class:"lang"},"bash"),e("pre",null,[e("code",{"v-pre":""},"opencode auth login")])],-1),n('<p><strong>你应该看到</strong>：</p><ul><li>选择器里的 provider 来自 models.dev 列表；你的自定义 provider（比如 <code>corp-gateway</code>）通常不会出现在这里</li><li>所以一般做法是：先选 <code>Other</code>，再输入 provider id：<code>corp-gateway</code>，然后进入你插件 <code>methods</code> 定义的登录流程</li></ul><hr><h2 id="检查点-✅" tabindex="-1">检查点 ✅ <a class="header-anchor" href="#检查点-✅" aria-label="Permalink to “检查点 ✅”">​</a></h2><ul><li>[ ] 方式 A：注入环境变量后，<code>opencode run -m corp-gateway/qwen2.5-72b ...</code> 能跑通</li><li>[ ] 方式 B：<code>https://&lt;host&gt;/.well-known/opencode</code> 能返回 JSON，且 <code>opencode auth login &lt;url&gt;</code> 能成功</li><li>[ ] 方式 C：启用插件后，<code>opencode auth login</code> 能走插件登录并保存凭据</li><li>[ ] 团队默认模型已经指向内网 Provider（<code>model: &quot;corp-gateway/...&quot;</code>）</li></ul><hr><h2 id="踩坑提醒" tabindex="-1">踩坑提醒 <a class="header-anchor" href="#踩坑提醒" aria-label="Permalink to “踩坑提醒”">​</a></h2><table tabindex="0"><thead><tr><th>现象</th><th>原因</th><th>解决</th></tr></thead><tbody><tr><td>登录后在另一个终端里 <code>echo $COMPANY_AI_TOKEN</code> 没有值</td><td>well-known 的 token 是在 OpenCode 启动时写入当前进程的 <code>process.env</code>，不会写进你的 <code>.zshrc/.bashrc</code></td><td>不影响 OpenCode 调用模型；用 <code>opencode run -m corp-gateway/...</code> 验证即可；如果你确实需要在 shell 里看到，得自己 <code>export COMPANY_AI_TOKEN=...</code></td></tr><tr><td><code>opencode auth login</code> 直接报 404/2000+</td><td><code>/.well-known/opencode</code> 不可达</td><td>先用浏览器/curl 验证 URL 可访问</td></tr><tr><td>提示 <code>Failed</code> / 没有 <code>Logged into ...</code></td><td><code>auth.command</code> 退出码非 0</td><td>确保命令能在当前机器运行，且 stdout 输出 Token</td></tr><tr><td>OpenCode 启动时报错并退出</td><td>远程 well-known 拉取失败会抛错，中断配置加载</td><td>先保证 <code>&lt;url&gt;/.well-known/opencode</code> 可达；排障时可用 <code>opencode auth logout</code> 移除这条 URL 凭据再启动</td></tr><tr><td><code>opencode auth login</code> 找不到你们的 provider</td><td>你们的 provider 不在 models.dev 列表</td><td>选 <code>Other</code>，手动输入 provider id（例如 <code>corp-gateway</code>）</td></tr><tr><td>登录成功但配置不生效</td><td>只保存了 Token，没有返回 <code>config</code></td><td>在 well-known JSON 里补上 <code>config</code> 字段</td></tr><tr><td>容器里能登录，本地不行（或反过来）</td><td>命令/证书链差异</td><td>确保容器和本机都装了公司根证书，并且 <code>auth.command</code> 在两边都可用</td></tr><tr><td>内网网关返回 401</td><td>Token 未被 provider 读取到或已过期</td><td>检查 <code>auth.env</code> 名称是否一致；用第 4 步 <code>opencode run -m ...</code> 复测；必要时重新 <code>opencode auth login &lt;url&gt;</code></td></tr><tr><td>插件放进 <code>.opencode/plugin/</code> 但没生效</td><td>依赖准备未完成：OpenCode 会对扫描到的目录调用 <code>installDependencies(dir)</code>。目录里没有 <code>package.json</code>/<code>.gitignore</code> 才会写入；并尝试执行 <code>bun add @opencode-ai/plugin@&lt;version&gt; --exact</code> 与 <code>bun install</code>（两条 <code>bun</code> 命令失败会被吞；写文件没有 catch）。只有当 <code>&lt;dir&gt;/node_modules</code> 不存在时才会等待安装流程；如果已存在则安装会后台触发、不等待完成</td><td>确保能访问 npm/内网镜像；必要时手动进对应目录执行 <code>bun install</code>，或把依赖提前打进镜像/缓存（源码：<code>packages/opencode/src/config/config.ts</code> 的 <code>installDependencies()</code>）</td></tr><tr><td>担心 well-known 不安全</td><td>远程 JSON 既能下发 <code>auth.command</code>（会在本机执行），也能下发 <code>config</code>（启动时会被拉取并合并；其中 <code>plugin</code> 可能触发插件安装与执行）</td><td>把 <code>/.well-known/opencode</code> 当成“组织发配置的入口”：只部署在可信内网域名；限制访问；对返回内容做审计；非必要不要在远程 config 里下发 <code>plugin</code></td></tr></tbody></table><hr><h2 id="本课小结" tabindex="-1">本课小结 <a class="header-anchor" href="#本课小结" aria-label="Permalink to “本课小结”">​</a></h2><p>你学会了：</p><ol><li>环境变量：最省事，适合 K8s/CI</li><li>well-known：最适合团队推广，统一登录和下发默认配置</li><li>认证插件：最灵活，适合 OAuth/刷新/签名/改 headers/params</li></ol><hr><h2 id="下一课预告" tabindex="-1">下一课预告 <a class="header-anchor" href="#下一课预告" aria-label="Permalink to “下一课预告”">​</a></h2><blockquote><p>下一课我们会把认证和网关能力做得更灵活：学会插件系统后，你可以写出真正“企业级”的自定义登录流程和请求改写。</p><p>下一课：<strong><a href="./12a-plugins-basics.html">5.12a 插件基础</a></strong>。</p></blockquote><hr><h2 id="附录-源码参考" tabindex="-1">附录：源码参考 <a class="header-anchor" href="#附录-源码参考" aria-label="Permalink to “附录：源码参考”">​</a></h2><details><summary><strong>点击展开查看源码位置</strong></summary><blockquote><p>更新时间：2026-02-05</p></blockquote><table tabindex="0"><thead><tr><th>功能</th><th>文件路径</th><th>行号</th></tr></thead><tbody><tr><td><code>opencode auth login &lt;url&gt;</code>：拉取 <code>/.well-known/opencode</code> 并执行 <code>auth.command</code>，保存 <code>type: wellknown</code> 凭据</td><td><a href="https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/cli/cmd/auth.ts#L231-L252" target="_blank" rel="noreferrer"><code>src/cli/cmd/auth.ts</code></a></td><td>231-252</td></tr><tr><td>启动时加载 well-known：把 token 写入 <code>auth.env</code> 环境变量，并合并远程 <code>config</code></td><td><a href="https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/config/config.ts#L62-L91" target="_blank" rel="noreferrer"><code>src/config/config.ts</code></a></td><td>62-91</td></tr><tr><td>认证信息存储结构（oauth/api/wellknown）与 <code>auth.json</code> 写入逻辑</td><td><a href="https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/auth/index.ts#L7-L69" target="_blank" rel="noreferrer"><code>src/auth/index.ts</code></a></td><td>7-69</td></tr><tr><td>插件认证钩子 <code>auth</code> 的类型定义（<code>methods</code>/<code>loader</code>）</td><td><a href="https://github.com/anomalyco/opencode/blob/dev/packages/plugin/src/index.ts#L37-L156" target="_blank" rel="noreferrer"><code>packages/plugin/src/index.ts</code></a></td><td>37-156</td></tr><tr><td>OpenCode 插件加载机制（内置插件 + 配置里的插件列表）</td><td><a href="https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/plugin/index.ts#L15-L90" target="_blank" rel="noreferrer"><code>src/plugin/index.ts</code></a></td><td>15-90</td></tr><tr><td><code>opencode run</code> 命令参数（<code>-m provider/model</code>）</td><td><a href="https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/cli/cmd/run.ts#L28-L94" target="_blank" rel="noreferrer"><code>src/cli/cmd/run.ts</code></a></td><td>28-94</td></tr></tbody></table><p><strong>关键类型</strong>：</p><ul><li><code>Auth.WellKnown</code>：<code>{ type: &quot;wellknown&quot;, key: string, token: string }</code></li><li><code>AuthHook</code>：插件认证接口，支持 <code>api</code> / <code>oauth</code> 两种 methods</li></ul></details>',18)])])}const w=t(i,[["render",s]]);export{_ as __pageData,w as default};
